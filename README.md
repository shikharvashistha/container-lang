# container-lang: A compiler for the container description language.


## Build the project
```bash
cargo build --release
```

## Run the Example

```bash
cargo run --release -- examples/app.container
```